% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_negbin_data}
\alias{simulate_negbin_data}
\title{Simulate Negative Binomial Data for Regression}
\usage{
simulate_negbin_data(n, p, beta_true, r, seed = NULL)
}
\arguments{
\item{n}{A positive integer specifying the number of observations to simulate.}

\item{p}{A positive integer specifying the number of predictors (including the intercept). The length of \code{beta_true}
should be equal to \code{p}.}

\item{beta_true}{A numeric vector of true regression coefficients of length \code{p}. The first element is the intercept.}

\item{r}{A positive numeric value representing the dispersion parameter for the Negative Binomial distribution.}

\item{seed}{An optional integer to set the seed for reproducibility. If \code{NULL}, the random number generator is not seeded.}
}
\value{
A list containing:
\describe{
  \item{y}{A numeric vector of simulated count responses.}
  \item{X}{A numeric matrix of predictors including an intercept column.}
}
}
\description{
This function generates synthetic data for Negative Binomial regression. It creates a predictor matrix
(including an intercept) and simulates count responses from a Negative Binomial distribution, using a
linear predictor based on the provided true regression coefficients.
}
\examples{
\dontrun{
# Set simulation parameters
n <- 100       # Number of observations
p <- 3         # Number of predictors (including intercept)
beta_true <- c(0.5, -1.2, 0.8)  # True regression coefficients
r <- 1.5       # Dispersion parameter
seed <- 123    # Seed for reproducibility

# Simulate data
sim_data <- simulate_negbin_data(n, p, beta_true, r, seed)
y <- sim_data$y
X <- sim_data$X

# Display a preview of the generated data
head(X)
head(y)
}

}
